var fs = require('fs');
var content = fs.readFile('./salutation.txt',
    function (err, res) {
        if (err) {
            console.log(err);
        console.log(result.toString());
        } 
    }
);
console.log('end of file!')